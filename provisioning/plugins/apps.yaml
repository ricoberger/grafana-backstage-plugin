apiVersion: 1

apps:
  - type: 'ricoberger-backstage-app'
    org_id: 1
    org_name: 'ricoberger'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key
