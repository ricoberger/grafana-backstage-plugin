apiVersion: 1

apps:
  - type: 'ricoberger-backstage-app'
    org_id: 1
    org_name: Main Org.
    disabled: false
    jsonData:
      url: https://demo.backstage.io
      dashboards:
        - [
            'Component',
            'component?var-name={{ $.metadata.name }}&var-namespace={{
            $.metadata.namespace }}&var-description={{ $.metadata.description
            }}&var-owner={{ $.spec.owner }}&var-system={{ $.spec.system
            }}&var-type={{ $.spec.type }}&var-lifecycle={{ $.spec.lifecycle
            }}&var-tags={{ $.metadata.tags }}',
          ]
    secureJsonData:
      apiKey:
